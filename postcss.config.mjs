/** Compiles the Tailwind utilities shipped by `@keystone-sites/widgets`.
 * Source scanning is confined in `design-system/widgets.css`. */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
