import { HomeNextPage } from "@/design-system/v2/home-next";

/** /home-next — the homepage v2 composition mounted bare (spec 018
 * §8); /home-next-fixture is the QA mount of the same composition. */
export default function HomeNext() {
  return <HomeNextPage />;
}
